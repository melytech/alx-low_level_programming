#include <stdio.h>
/**
* main - Write a program that prints the alphabet in lowercase, and then in uppercase
* Return: Always true
/*
int main(void)
{
char alpha = 97;

	while (alpha <= 122)
	{
		putchar(alpha++);
	}

	alpha = 65;

	while (alpha <= 90)
	{
		putchar(alpha++);
	}
	putchar('\n');

	return (0);
}
